interface:
  display_name: "MarkItDown"
  short_description: "Convert files and batches to Markdown"
  default_prompt: "Use $markitdown to convert these documents into Markdown files."
policy:
  allow_implicit_invocation: true
